# Model Configurations

This directory contains model architecture configurations.

## Structure

- `encoder/` - BERT, RoBERTa, DeBERTa, etc.
- `decoder/` - GPT-2, LLaMA, Mistral, Phi, etc.
- `encoder_decoder/` - T5, BART, etc.
- `custom/` - Custom model architectures
